/**
 * Arquivo de configuração em tempo de execução.
 * Edite APENAS a variável apiUrl abaixo, colocando a URL pública do seu backend.
 *
 * Exemplos:
 *   "https://meu-backend.railway.app"
 *   "https://api.meudominio.com.br"
 *
 * Deixe em branco ("") para usar o mesmo domínio do frontend (só funciona se
 * o backend estiver no mesmo servidor/domínio que o frontend).
 * http://localhost:8080
 */
window.APP_CONFIG = {
  apiUrl: ""
};
